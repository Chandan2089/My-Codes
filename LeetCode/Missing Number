class Solution(object):
    def missingNumber(self, nums):
        n = len(nums)
        
        total = n * (n + 1) // 2
        
        for i in nums:
            total = total - i
        
        return total
        
